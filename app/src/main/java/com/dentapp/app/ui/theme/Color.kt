package com.dentapp.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary ───────────────────────────────────────────────────────────────────
val TealPrimary     = Color(0xFF00BFA5)
val TealDark        = Color(0xFF00897B)
val TealLight       = Color(0xFFB2DFDB)

// ── Accent ────────────────────────────────────────────────────────────────────
val CoralAccent     = Color(0xFFFF6B6B)
val CoralLight      = Color(0xFFFFE5E5)

// ── Surface ───────────────────────────────────────────────────────────────────
val Background      = Color(0xFFF5F7FA)
val SurfaceWhite    = Color(0xFFFFFFFF)
val SurfaceGray     = Color(0xFFF0F2F5)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary     = Color(0xFF0D1117)
val TextSecondary   = Color(0xFF6B7280)
val TextTertiary    = Color(0xFF9CA3AF)

// ── Gradient ──────────────────────────────────────────────────────────────────
val GradientStart   = Color(0xFF00BFA5)
val GradientEnd     = Color(0xFF0077B6)

// ── Semantic ──────────────────────────────────────────────────────────────────
val AlertRed        = Color(0xFFDC2626)
val AlertRedLight   = Color(0xFFFEE2E2)
val AlertAmber      = Color(0xFFF59E0B)
val AlertAmberLight = Color(0xFFFEF3C7)
val SuccessGreen    = Color(0xFF10B981)

// ── Legacy aliases (kept for screens not yet migrated) ────────────────────────
val Primary         = TealPrimary
val PrimaryLight    = TealLight
val PrimaryDark     = TealDark
val Secondary       = GradientEnd
val SecondaryLight  = Color(0xFFE0F2FE)
val Surface         = SurfaceWhite
val Error           = AlertRed
val Success         = SuccessGreen
val Warning         = AlertAmber
val OnPrimary       = Color(0xFFFFFFFF)
val OnBackground    = TextPrimary
val CardBackground  = SurfaceWhite
val DividerColor    = SurfaceGray
val DentBlue        = TealPrimary
val DentBlueDark    = TealDark
val DentBlueLight   = TealLight
val DentGreen       = SuccessGreen
val DentGreenDark   = Color(0xFF059669)
val DentGray        = SurfaceGray
val DentTextPrimary = TextPrimary
val DentTextSecond  = TextSecondary
val White           = Color(0xFFFFFFFF)
val ErrorRed        = AlertRed
